name: PR CI/CD: Build & Argo CD Sync

on:
  pull_request:
    branches:
      - main
      - develop

jobs:
  build-and-push:
    name: Build Docker Image & Push
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker image
        run: |
          docker build -t n8n-custom:pr-${{ github.event.pull_request.number }} ./compose

      - name: Push Docker image
        run: |
          docker tag n8n-custom:pr-${{ github.event.pull_request.number }} ${{ secrets.DOCKER_USERNAME }}/n8n-custom:pr-${{ github.event.pull_request.number }}
          docker push ${{ secrets.DOCKER_USERNAME }}/n8n-custom:pr-${{ github.event.pull_request.number }}

      - name: Update Helm values for PR branch
        run: |
          sed -i "s/tag: .*/tag: pr-${{ github.event.pull_request.number }}/" helm/values-dev.yaml
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add helm/values-dev.yaml
          git commit -m "Update image tag for PR #${{ github.event.pull_request.number }}"
          git push origin HEAD:${{ github.head_ref }}

  argo-sync:
    name: Argo CD Sync
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Argo CD CLI
        run: |
          curl -sSL -o argocd https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          chmod +x argocd
          sudo mv argocd /usr/local/bin/

      - name: Log in to Argo CD
        run: |
          argocd login localhost:8080 \
            --username admin \
            --password ${{ secrets.ARGO_PASSWORD }} \
            --insecure

      - name: Sync n8n PR environment
        run: |
          argocd app sync n8n-dev
          argocd app wait n8n-dev --health

